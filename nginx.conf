server {
    listen 8080;

    root /app;
    index index.html;

    # SvelteKit static adapter produces /akty/index.html — serve it cleanly
    location / {
        try_files $uri $uri/ $uri.html /index.html;
    }

    # Long-lived cache for hashed immutable assets
    location /_app/immutable/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Medium cache for static images
    location /img/ {
        expires 30d;
        add_header Cache-Control "public, must-revalidate";
    }

    # Compression
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_buffers 16 8k;
    gzip_http_version 1.1;
    gzip_min_length 256;
    gzip_types
        application/atom+xml
        application/geo+json
        application/javascript
        application/x-javascript
        application/json
        application/ld+json
        application/manifest+json
        application/rdf+xml
        application/rss+xml
        application/xhtml+xml
        application/xml
        font/eot
        font/otf
        font/ttf
        image/svg+xml
        text/css
        text/javascript
        text/plain
        text/xml;
}
